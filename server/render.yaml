services:
  - type: web
    name: prime-ai
    runtime: python

    buildCommand: |
      pip install uv
      uv sync

    startCommand: |
      uv run uvicorn server.main:app --host 0.0.0.0 --port $PORT
